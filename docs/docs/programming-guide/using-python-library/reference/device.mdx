---
sidebar_position: 10
sidebar_label: Device API
---

# Device API

:::info[Generated reference]
This page is generated from the Python source under `python/t76`. Do not edit it by hand; update source docstrings or the generator instead.
:::

Discovery, device connection, SCPI-backed controllers, events, and device data types.

## `t76.drpd.device`

**Source:** `python/t76/drpd/device/__init__.py`

**Exports:** `AccumulatedMeasurements`, `AnalogMonitorChannels`, `BatteryCapabilitiesInquiryData`, `BatteryCapacity`, `BatteryCapacityMeaning`, `BatteryChargingState`, `BatteryInquiryFailureAction`, `BatteryReferenceKind`, `BatteryStatusInquiryData`, `BatterySurveyResult`, `CableDiscoverIdentityInquiryRequest`, `CableDiscoverModesInquiryRequest`, `CableDiscoverSVIDsInquiryRequest`, `CableManufacturerInfoInquiryRequest`, `CablePlug`, `CableRevisionInquiryRequest`, `CableStatusInquiryData`, `CableStatusInquiryRequest`, `CCBusState`, `CCChannel`, `Device`, `DeviceInfo`, `DeviceStatusFlags`, `DiagnosticCCChannel`, `DiagnosticCCRole`, `DiscoverIdentityInquiryData`, `DiscoverIdentityInquiryRequest`, `DiscoverModesInquiryData`, `DiscoverModesInquiryRequest`, `DiscoverSVIDsInquiryData`, `DiscoverSVIDsInquiryRequest`, `CountryCodesInquiryData`, `CountryInfoInquiryData`, `CountryInquiryFailureAction`, `CountryInquiryWorkflowResult`, `ExtendedSourceCapabilitiesInquiryData`, `GetExtendedSourceCapabilitiesInquiryRequest`, `GetBatteryCapabilitiesInquiryRequest`, `GetBatteryStatusInquiryRequest`, `GetCountryCodesInquiryRequest`, `GetCountryInfoInquiryRequest`, `GetManufacturerInfoInquiryRequest`, `GetPPSStatusInquiryRequest`, `GetRevisionInquiryRequest`, `GetSourceCapabilitiesInquiryRequest`, `GetSourceInfoInquiryRequest`, `GetStatusInquiryRequest`, `MemoryUsage`, `Mode`, `ManufacturerInfoInquiryData`, `ManufacturerInfoTarget`, `OnOffStatus`, `ResistorStatus`, `SinkInquiryOutcome`, `SinkInquiryDecodedData`, `SinkInquiryRequest`, `SinkInquiryResult`, `SinkInquiryRunner`, `SinkInquiryStatus`, `SinkInquirySupersededError`, `SinkInquiryType`, `PPSStatusInquiryData`, `RevisionInquiryData`, `SinkRequestOutcome`, `SinkRequestStatus`, `SinkState`, `SourceCapabilitiesInquiryData`, `SourceInfoInquiryData`, `SourceStatusInquiryData`, `StructuredVDMHeaderData`, `StructuredVDMNegativeResponseData`, `TriggerMessageTypeFilter`, `TriggerMessageTypeFilterClass`, `TriggerSenderFilter`, `TriggerStatus`, `TriggerSyncMode`, `TriggerType`, `VBusState`, `VDMDiscoveryFailureAction`, `VDMDiscoveryWorkflowResult`, `find_drpd_devices`

No public classes or functions defined directly in this module.

## `t76.drpd.device.authentication`

**Source:** `python/t76/drpd/device/authentication.py`

Deterministic USB Type-C Authentication verification primitives.

### `t76.drpd.device.authentication.AuthenticationTrustAnchor`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Explicit offline USB authentication root; OS/Web PKI is never consulted.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `root_certificate_der` | bytes | — |
| `root_hash` | bytes | — |
| `disabled` | bool | False |
| `denied_fingerprints` | frozenset[bytes] | frozenset() |




### `t76.drpd.device.authentication.AuthenticationVerificationError`

**Kind:** class

**Bases:** `ValueError`

Failure tagged by transport/parse/signature/chain/trust/policy layer.





### `t76.drpd.device.authentication.generate_challenge_nonce`

**Kind:** function

```python
def generate_challenge_nonce() -> bytes
```

Return one fresh 32-byte platform-CSPRNG nonce.





### `t76.drpd.device.authentication.certificate_chain_digest`

**Kind:** function

```python
def certificate_chain_digest(chain: bytes) -> bytes
```

Return the protocol SHA-256 digest of the exact chain bytes.





### `t76.drpd.device.authentication.signature_little_endian_to_p1363`

**Kind:** function

```python
def signature_little_endian_to_p1363(signature: bytes) -> bytes
```

Convert USB Auth little-endian r||s to fixed-width big-endian P1363.





### `t76.drpd.device.authentication.challenge_transcript`

**Kind:** function

```python
def challenge_transcript(request_body: bytes, response_body: bytes) -> bytes
```

Build the exact signed transcript: 36 request bytes + first 104 response bytes.





### `t76.drpd.device.authentication.verify_challenge_signature`

**Kind:** function

```python
def verify_challenge_signature(public_key: object, request_body: bytes, response_body: bytes) -> None
```

Verify ECDSA-P256/SHA-256; raises a layer-specific deterministic failure.





### `t76.drpd.device.authentication.verify_certificate_chain`

**Kind:** function

```python
def verify_certificate_chain(certificate_chain: tuple[bytes, ...], anchor: AuthenticationTrustAnchor | None, *, expected_vid: int | None = None, expected_pid: int | None = None, wire_root_hash: bytes | None = None, slot: int | None = None, allowed_slots: frozenset[int] | None = None) -> x509.Certificate
```

Verify exact wire-order product chain against one explicit offline anchor.





## `t76.drpd.device.device`

**Source:** `python/t76/drpd/device/device.py`

Copyright (c) 2025 MTA, Inc.

The Device class enables communication with DRPD devices
over USB using SCPI commands.

### `t76.drpd.device.device.Device`

**Kind:** class

Represents a DRPD device connected via USB over USBTMC.

:param usb_device: The USB device object representing the DRPD device.
:type usb_device: usb.core.Device



#### Public properties

| Property | Type | Access | Summary |
| --- | --- | --- | --- |
| `name` | Optional[str] | read-only | Get the name of the device. |


#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `connect` | `async def connect(self) -> None` | Connect to the device and set up event handling. |
| `disconnect` | `async def disconnect(self) -> None` | Disconnect from the device and clean up event handling. |
| `register_event_observer` | `def register_event_observer(self, observer: DeviceEvents.EventObserver) -> None` | Register an observer to be notified of events. |
| `unregister_event_observer` | `def unregister_event_observer(self, observer: DeviceEvents.EventObserver) -> None` | Unregister an observer from receiving event notifications. |
| `send_encoder_message` | `async def send_encoder_message(self, header: Header, message: Optional[Message] = None) -> None` | Send an encoded message using the device's encoder. |


## `t76.drpd.device.device_analog_monitor`

**Source:** `python/t76/drpd/device/device_analog_monitor.py`

Copyright (c) 2025 MTA, Inc.

The Device class enables communication with DRPD devices
over USB using SCPI commands.

### `t76.drpd.device.device_analog_monitor.DeviceAnalogMonitor`

**Kind:** class

Represents the analog monitor-related commands for a DRPD device.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `VBUS_CALIBRATION_POINT_COUNT` | — | 61 |
| `VBUS_CURRENT_CALIBRATION_POINT_COUNT` | — | 13 |
| `VBUS_CURRENT_CALIBRATION_INTERVAL_MA` | — | 500 |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `get_status` | `async def get_status(self) -> AnalogMonitorChannels` | Get the analog monitor voltages from the device. |
| `get_accumulated_measurements` | `async def get_accumulated_measurements(self) -> AccumulatedMeasurements` | Query accumulated VBUS charge and energy counters. |
| `reset_accumulated_measurements` | `async def reset_accumulated_measurements(self) -> None` | Reset accumulated VBUS charge and energy counters. |
| `get_vbus_voltage` | `async def get_vbus_voltage(self) -> float` | — |
| `get_vbus_current` | `async def get_vbus_current(self) -> float` | — |
| `get_dut_cc1_voltage` | `async def get_dut_cc1_voltage(self) -> float` | — |
| `get_dut_cc2_voltage` | `async def get_dut_cc2_voltage(self) -> float` | — |
| `get_usds_cc1_voltage` | `async def get_usds_cc1_voltage(self) -> float` | — |
| `get_usds_cc2_voltage` | `async def get_usds_cc2_voltage(self) -> float` | — |
| `get_adc_reference_voltage` | `async def get_adc_reference_voltage(self) -> float` | — |
| `get_current_reference_voltage` | `async def get_current_reference_voltage(self) -> float` | — |
| `get_ground_reference_voltage` | `async def get_ground_reference_voltage(self) -> float` | — |
| `get_vbus_calibration_table` | `async def get_vbus_calibration_table(self) -> List[float]` | Return the persisted VBUS calibration correction table. |
| `calibrate_vbus_bucket` | `async def calibrate_vbus_bucket(self, bucket: int) -> None` | Capture a calibration point for the specified raw-voltage bucket. |
| `set_vbus_calibration_table_point` | `async def set_vbus_calibration_table_point(self, bucket: int, correction: float) -> None` | Set one persisted VBUS correction table entry directly. |
| `set_vbus_calibration_table` | `async def set_vbus_calibration_table(self, table: List[float]) -> None` | Replace the persisted VBUS correction table with explicit values. |
| `reset_vbus_calibration_to_defaults` | `async def reset_vbus_calibration_to_defaults(self) -> None` | Restore the persisted VBUS calibration table to firmware defaults. |
| `get_raw_vbus_current` | `async def get_raw_vbus_current(self) -> float` | Return the latest raw scaled VBUS current before calibration. |
| `get_vbus_current_calibration_table` | `async def get_vbus_current_calibration_table(self) -> List[float]` | Return the persisted VBUS current raw calibration table. |
| `calibrate_vbus_current_bucket` | `async def calibrate_vbus_current_bucket(self, target_ma: int) -> None` | Capture a calibration point for the specified raw current. |
| `set_vbus_current_calibration_table_point` | `async def set_vbus_current_calibration_table_point(self, target_ma: int, raw_current_a: float) -> None` | Set one persisted VBUS current raw calibration entry directly. |
| `set_vbus_current_calibration_table` | `async def set_vbus_current_calibration_table(self, table: List[float]) -> None` | Replace the persisted VBUS current raw calibration table. |
| `reset_vbus_current_calibration_to_defaults` | `async def reset_vbus_current_calibration_to_defaults(self) -> None` | Restore the persisted VBUS current calibration table to firmware defaults. |
| `start_recurring_status_updates` | `async def start_recurring_status_updates(self, frequency: float) -> None` | Start a recurring task that periodically fetches analog monitor |
| `stop_recurring_status_updates` | `async def stop_recurring_status_updates(self) -> None` | Stop the recurring status update task. |


## `t76.drpd.device.device_capture`

**Source:** `python/t76/drpd/device/device_capture.py`

Copyright (c) 2025 MTA, Inc.

The Device class enables communication with DRPD devices
over USB using SCPI commands.

### `t76.drpd.device.device_capture.DeviceCapture`

**Kind:** class

Provides methods to control and retrieve data captures from a DRPD device.
:param internal: The internal device communication handler.
:type internal: DeviceInternal


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `CapturedRecord` | — | Union[BMCSequence, FirmwareCaptureEvent] |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `start` | `async def start(self) -> None` | Start capturing data on the device. |
| `stop` | `async def stop(self) -> None` | Stop capturing data on the device. |
| `get_status` | `async def get_status(self) -> OnOffStatus` | Get the current capture status of the device. |
| `get_cycle_length` | `async def get_cycle_length(self) -> float` | Get the current capture cycle length of the device. |
| `reset` | `async def reset(self) -> None` | Reset the capture data on the device. |
| `get_capture_count` | `async def get_capture_count(self) -> int` | Get the number of available captures on the device. |
| `fetch_extant_captures` | `async def fetch_extant_captures(self) -> None` | Fetch all extant captures from the device and notify observers. |


## `t76.drpd.device.device_events`

**Source:** `python/t76/drpd/device/device_events.py`

Copyright (c) 2025 MTA, Inc.

The DeviceEventManager class manages event observation and dispatching
for DRPD devices.

### `t76.drpd.device.device_events.DeviceEvents`

**Kind:** class

Manages event observation and dispatching for a DRPD device.

Provides functionality to register/unregister event observers and
dispatch events to all registered observers. Supports both
synchronous and asynchronous observer callbacks.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `EventObserver` | — | Union[Callable[[DeviceEvent], None], Callable[[DeviceEvent], Awaitable[None]]] |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `get_observer_count` | `def get_observer_count(self) -> int` | Get the number of registered event observers. |
| `register_event_observer` | `def register_event_observer(self, observer: EventObserver) -> None` | Register an observer to be notified of events. |
| `unregister_event_observer` | `def unregister_event_observer(self, observer: EventObserver) -> None` | Unregister an observer from receiving event notifications. |
| `dispatch_event` | `async def dispatch_event(self, event: DeviceEvent) -> None` | Dispatch an event to all registered observers. |


## `t76.drpd.device.device_internal`

**Source:** `python/t76/drpd/device/device_internal.py`

Copyright (c) 2025 MTA, Inc.

The Device class enables communication with DRPD devices
over USB using SCPI commands.

### `t76.drpd.device.device_internal.USBTMCTransportError`

**Kind:** class

**Bases:** `RuntimeError`

Raised when the low-level USBTMC transport fails.





### `t76.drpd.device.device_internal.DeviceInternal`

**Kind:** class

Internal class for handling low-level USB SCPI communication.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `LOCK_TIMEOUT_SECONDS` | — | 2.0 |


#### Public properties

| Property | Type | Access | Summary |
| --- | --- | --- | --- |
| `connected` | bool | read-only | — |


#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `parse_scpi_string` | `def parse_scpi_string(cls, scpi_string: str) -> str` | Interpret a SCPI string by removing leading and trailing quotation |
| `check_for_error` | `def check_for_error(self, command: str) -> None` | Query the instrument for errors and raise when one is reported. |
| `write_ascii_and_check` | `async def write_ascii_and_check(self, command: str) -> None` | Write a command to the instrument and check for errors. |
| `write_binary_and_check` | `async def write_binary_and_check(self, command: str, data: Sequence[int \| float], datatype: BINARY_DATATYPE = 'B') -> None` | Write binary data to the instrument and check for errors. |
| `query_ascii_values_and_check` | `async def query_ascii_values_and_check(self, command: str, converter: ASCII_CONVERTER = 'f') -> Sequence[Any]` | Query the instrument for ASCII values. |
| `query_binary_value_and_check` | `async def query_binary_value_and_check(self, command: str, datatype: BINARY_DATATYPE = 'B', container: type = list) -> Sequence[int \| float]` | Query the instrument for binary values. |
| `connect` | `async def connect(self) -> None` | Connect to the device using the underlying transport. |
| `disconnect` | `async def disconnect(self) -> None` | Disconnect from the device. |


## `t76.drpd.device.device_mode`

**Source:** `python/t76/drpd/device/device_mode.py`

Copyright (c) 2025 MTA, Inc.

The Device class enables communication with DRPD devices
over USB using SCPI commands.

### `t76.drpd.device.device_mode.DeviceMode`

**Kind:** class

Represents the device mode-related commands for a DRPD device.




#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `get` | `async def get(self) -> Mode` | Get the current mode of the device. |
| `set` | `async def set(self, mode: Mode) -> None` | Set the mode of the device. |
| `get_status` | `async def get_status(self) -> CCBusState` | Get the current status of the device mode. |


## `t76.drpd.device.device_sink`

**Source:** `python/t76/drpd/device/device_sink.py`

Copyright (c) 2025 MTA, Inc.

The Device class enables communication with DRPD devices
over USB using SCPI commands.

### `t76.drpd.device.device_sink.SinkInfo`

**Kind:** dataclass

**Decorators:** `dataclass`

Represents comprehensive information about the sink system of the DRPD device.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `status` | SinkState | — |
| `negotiated_pdo` | Optional[DeviceSinkPDO] | — |
| `negotiated_voltage` | float | — |
| `negotiated_current` | float | — |
| `error_status` | bool | — |




### `t76.drpd.device.device_sink.SinkInquirySupersededError`

**Kind:** class

**Bases:** `RuntimeError`

Raised when firmware's latest result no longer belongs to this run.





### `t76.drpd.device.device_sink.SinkInquiryRunner`

**Kind:** class

Serialize, correlate, and retain semantic Sink inquiry executions.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `MAX_HISTORY` | — | 256 |
| `MAX_POLLS` | — | 10000 |
| `MAX_POLL_INTERVAL_SECONDS` | — | 60.0 |


#### Public properties

| Property | Type | Access | Summary |
| --- | --- | --- | --- |
| `history` | tuple[SinkInquiryResult, ...] | read-only | Return oldest-to-newest completed host-side inquiry history. |


#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `run` | `async def run(self, request: SinkInquiryRequest, *, poll_interval_seconds: float = 0.01, max_polls: int = 1000) -> SinkInquiryResult` | Run one inquiry and return its correlated terminal result. |
| `run_country_information` | `async def run_country_information(self, country_codes: tuple[str, ...] \| None = None, *, failure_action: CountryInquiryFailureAction = CountryInquiryFailureAction.STOP, max_retries: int = 1, max_countries: int = 12, poll_interval_seconds: float = 0.01, max_polls: int = 1000) -> CountryInquiryWorkflowResult` | Enumerate country codes, then fetch selected or all country data. |
| `run_battery_survey` | `async def run_battery_survey(self, battery_references: tuple[int, ...] \| None = None, *, extended_source_capabilities: ExtendedSourceCapabilitiesInquiryData \| None = None, failure_action: BatteryInquiryFailureAction = BatteryInquiryFailureAction.STOP, max_retries: int = 1, poll_interval_seconds: float = 0.01, max_polls: int = 1000) -> BatterySurveyResult` | Survey Battery Capabilities then Status for bounded references. |
| `run_vdm_discovery` | `async def run_vdm_discovery(self, selected_svids: tuple[int, ...] \| None = None, *, failure_action: VDMDiscoveryFailureAction = VDMDiscoveryFailureAction.STOP, max_retries: int = 1, max_svids: int = 64, max_svid_pages: int = 8, poll_interval_seconds: float = 0.01, max_polls: int = 1000, _cable_plug: CablePlug \| None = None) -> VDMDiscoveryWorkflowResult` | Diagnose SOP partner from UFP/Sink; SVIDs/Modes are optional. |
| `run_cable_vdm_discovery` | `async def run_cable_vdm_discovery(self, plug: CablePlug, selected_svids: tuple[int, ...] \| None = None, *, failure_action: VDMDiscoveryFailureAction = VDMDiscoveryFailureAction.STOP, max_retries: int = 1, max_svids: int = 64, max_svid_pages: int = 8, poll_interval_seconds: float = 0.01, max_polls: int = 1000) -> VDMDiscoveryWorkflowResult` | Discover one explicit cable plug; never falls back to SOP. |


### `t76.drpd.device.device_sink.DeviceSink`

**Kind:** class

Represents the sink system of the DRPD device.




#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `load_config` | `async def load_config(self, config: dict) -> None` | Load the sink configuration from a dictionary. |
| `save_config` | `async def save_config(self) -> dict` | Save the current sink configuration to a dictionary. |
| `get_pdo_count` | `async def get_pdo_count(self) -> int` | Get the number of available PDOs from the source. |
| `get_pdo_at_index` | `async def get_pdo_at_index(self, index: int) -> Optional[DeviceSinkPDO]` | Get the PDO at the specified index. |
| `set_pdo` | `async def set_pdo(self, index: int, voltage_mv: int, current_ma: int) -> None` | Request a Fixed Supply PDO at the specified index with a |
| `set_epr_enabled` | `async def set_epr_enabled(self, enabled: bool) -> None` | Set whether Sink policy may enter EPR mode during future negotiation. |
| `get_epr_enabled` | `async def get_epr_enabled(self) -> bool` | Get whether Sink policy may enter EPR mode during negotiation. |
| `set_pps_status_query_enabled` | `async def set_pps_status_query_enabled(self, enabled: bool) -> None` | Set whether Sink policy sends Get_PPS_Status after SPR PPS transitions. |
| `get_pps_status_query_enabled` | `async def get_pps_status_query_enabled(self) -> bool` | Query whether Sink policy sends Get_PPS_Status after PPS transitions. |
| `get_request_status` | `async def get_request_status(self) -> SinkRequestStatus` | Query the most recent Sink PDO request outcome. |
| `send_inquiry` | `async def send_inquiry(self, inquiry: SinkInquiryType \| SinkInquiryRequest) -> None` | Start an enum-compatible or semantic Sink-to-Source inquiry. |
| `get_inquiry_status` | `async def get_inquiry_status(self) -> SinkInquiryStatus` | Query the most recent Sink-to-Source inquiry status. |
| `get_inquiry_response` | `async def get_inquiry_response(self) -> bytes` | Fetch the raw response bytes for the most recent inquiry. |
| `run_inquiry` | `async def run_inquiry(self, request: SinkInquiryRequest, *, poll_interval_seconds: float = 0.01, max_polls: int = 1000) -> SinkInquiryResult` | Run one semantic inquiry through this Sink's serialized runner. |
| `get_spr_capability_count` | `async def get_spr_capability_count(self) -> int` | — |
| `get_spr_capability_pdo` | `async def get_spr_capability_pdo(self, index: int) -> int` | — |
| `set_spr_capability_pdo` | `async def set_spr_capability_pdo(self, index: int, raw_pdo: int) -> None` | — |
| `get_epr_capability_count` | `async def get_epr_capability_count(self) -> int` | — |
| `get_epr_capability_pdo` | `async def get_epr_capability_pdo(self, index: int) -> int` | — |
| `set_epr_capability_pdo` | `async def set_epr_capability_pdo(self, index: int, raw_pdo: int) -> None` | — |
| `get_status` | `async def get_status(self) -> SinkState` | Get the current state of the sink state machine. |
| `get_negotiated_pdo` | `async def get_negotiated_pdo(self) -> Optional[DeviceSinkPDO]` | Get information about the negotiated PDO. |
| `get_negotiated_voltage` | `async def get_negotiated_voltage(self) -> int` | Get the negotiated voltage in millivolts. |
| `get_negotiated_current` | `async def get_negotiated_current(self) -> int` | Get the negotiated current in milliamps. |
| `get_error_status` | `async def get_error_status(self) -> bool` | Get the error status of the sink. |
| `get_sink_info` | `async def get_sink_info(self) -> SinkInfo` | Get comprehensive information about the sink system. |


## `t76.drpd.device.device_sink_pdos`

**Source:** `python/t76/drpd/device/device_sink_pdos.py`

Copyright (c) 2025 MTA, Inc.

This module defines classes for representing Power Data Objects (PDOs)
from SINK:PDO? SCPI command responses. It supports all six PDO types:
Fixed Supply, Variable Supply, Battery Supply, SPR PPS, SPR AVS, and EPR AVS.

### `t76.drpd.device.device_sink_pdos.DeviceSinkPDO`

**Kind:** dataclass

**Bases:** `ABC`

**Decorators:** `dataclass(frozen=True)`

Base class for all Sink Power Data Objects (PDO) from SCPI
responses.

This class represents a PDO returned by the SINK:PDO? SCPI query
command. Subclasses implement specific PDO types, each with
appropriate voltage, current, or power properties.

The class supports factory construction from SCPI response strings
with transparent polymorphism—the appropriate subclass is
automatically instantiated based on the response format.




#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_response` | `def from_response(response: Sequence[str]) -> Optional['DeviceSinkPDO']` | Parse an SCPI SINK:PDO? response and return the appropriate |
| `to_dict` | `def to_dict(self) -> dict[str, Any]` | Serialize the PDO to a dictionary representation. |


### `t76.drpd.device.device_sink_pdos.FixedPDO`

**Kind:** dataclass

**Bases:** `DeviceSinkPDO`

**Decorators:** `dataclass(frozen=True)`

Represents a Fixed Supply PDO from a SINK:PDO? SCPI response.

A Fixed Supply PDO indicates the source can provide a fixed,
well-regulated voltage at the specified current level.

Attributes:
    voltage: The fixed supply voltage in volts (float).
    max_current: The maximum available current in amps (float).


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `voltage` | float | — |
| `max_current` | float | — |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `to_dict` | `def to_dict(self) -> dict[str, Any]` | Serialize the Fixed PDO to a dictionary. |


### `t76.drpd.device.device_sink_pdos.VariablePDO`

**Kind:** dataclass

**Bases:** `DeviceSinkPDO`

**Decorators:** `dataclass(frozen=True)`

Represents a Variable Supply PDO from a SINK:PDO? SCPI response.

A Variable Supply PDO indicates the source can regulate voltage
within a specified range at the specified current level.

Attributes:
    min_voltage: The minimum voltage in volts (float).
    max_voltage: The maximum voltage in volts (float).
    max_current: The maximum available current in amps (float).


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `min_voltage` | float | — |
| `max_voltage` | float | — |
| `max_current` | float | — |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `to_dict` | `def to_dict(self) -> dict[str, Any]` | Serialize the Variable PDO to a dictionary. |


### `t76.drpd.device.device_sink_pdos.BatteryPDO`

**Kind:** dataclass

**Bases:** `DeviceSinkPDO`

**Decorators:** `dataclass(frozen=True)`

Represents a Battery Supply PDO from a SINK:PDO? SCPI response.

A Battery Supply PDO indicates the source is a battery that can
regulate voltage within a specified range. Power delivery is
limited by maximum power rather than current.

Attributes:
    min_voltage: The minimum voltage in volts (float).
    max_voltage: The maximum voltage in volts (float).
    max_power: The maximum available power in watts (float).


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `min_voltage` | float | — |
| `max_voltage` | float | — |
| `max_power` | float | — |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `to_dict` | `def to_dict(self) -> dict[str, Any]` | Serialize the Battery PDO to a dictionary. |


### `t76.drpd.device.device_sink_pdos.SPR_PDOPPS`

**Kind:** dataclass

**Bases:** `DeviceSinkPDO`

**Decorators:** `dataclass(frozen=True)`

Represents an SPR PPS (Programmable Power Supply) PDO from a
SINK:PDO? SCPI response.

An SPR PPS PDO allows the sink to request specific voltage and
current within the advertised range with finer granularity than
fixed PDOs.

Attributes:
    min_voltage: The minimum voltage in volts (float).
    max_voltage: The maximum voltage in volts (float).
    max_current: The maximum available current in amps (float).


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `min_voltage` | float | — |
| `max_voltage` | float | — |
| `max_current` | float | — |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `to_dict` | `def to_dict(self) -> dict[str, Any]` | Serialize the SPR PPS PDO to a dictionary. |


### `t76.drpd.device.device_sink_pdos.SPR_PDOAVs`

**Kind:** dataclass

**Bases:** `DeviceSinkPDO`

**Decorators:** `dataclass(frozen=True)`

Represents an SPR AVS (Adjustable Voltage Supply) PDO from a
SINK:PDO? SCPI response.

An SPR AVS PDO indicates the source can regulate voltage within a
specified range with separate current limits for its two voltage bands.

Attributes:
    min_voltage: The minimum voltage in volts (float).
    max_voltage: The maximum voltage in volts (float).
    max_current_15v: Maximum current for 9V through 15V, in amps.
    max_current_20v: Maximum current above 15V through 20V, in amps.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `min_voltage` | float | — |
| `max_voltage` | float | — |
| `max_current_15v` | float | — |
| `max_current_20v` | float | — |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `to_dict` | `def to_dict(self) -> dict[str, Any]` | Serialize the SPR AVS PDO to a dictionary. |


### `t76.drpd.device.device_sink_pdos.EPR_PDOAVs`

**Kind:** dataclass

**Bases:** `DeviceSinkPDO`

**Decorators:** `dataclass(frozen=True)`

Represents an EPR AVS (Adjustable Voltage Supply) PDO from a
SINK:PDO? SCPI response.

An EPR AVS PDO indicates the source is an extended power range
supply that can regulate voltage within a specified range. Power
delivery is limited by maximum power rather than current.

Attributes:
    min_voltage: The minimum voltage in volts (float).
    max_voltage: The maximum voltage in volts (float).
    max_power: The maximum available power in watts (float).


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `min_voltage` | float | — |
| `max_voltage` | float | — |
| `max_power` | float | — |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `to_dict` | `def to_dict(self) -> dict[str, Any]` | Serialize the EPR AVS PDO to a dictionary. |


## `t76.drpd.device.device_system`

**Source:** `python/t76/drpd/device/device_system.py`

Copyright (c) 2025 MTA, Inc.

The Device class enables communication with DRPD devices
over USB using SCPI commands.

### `t76.drpd.device.device_system.DeviceBMCDecoderConfiguration`

**Kind:** class

Manage persisted BMC decoder physical-layer settings.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `VREF_MIN_VOLTS` | — | 0.2 |
| `VREF_MAX_VOLTS` | — | 2.5 |
| `VREF_STEP_VOLTS` | — | 0.05 |
| `VREF_DEFAULT_VOLTS` | — | 0.7 |
| `PWM_MIN_HZ` | — | 10000 |
| `PWM_MAX_HZ` | — | 500000 |
| `PWM_STEP_HZ` | — | 1000 |
| `PWM_DEFAULT_HZ` | — | 100000 |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `get_cc_vref_voltage` | `async def get_cc_vref_voltage(self) -> float` | Return persisted CC reference voltage in volts. |
| `set_cc_vref_voltage` | `async def set_cc_vref_voltage(self, voltage: float) -> None` | Validate, persist, and immediately apply CC reference voltage. |
| `reset_cc_vref_voltage` | `async def reset_cc_vref_voltage(self) -> None` | Restore and persist build-time CC reference voltage default. |
| `get_cc_vref_pwm_frequency_hz` | `async def get_cc_vref_pwm_frequency_hz(self) -> int` | Return persisted CC reference PWM frequency in hertz. |
| `set_cc_vref_pwm_frequency_hz` | `async def set_cc_vref_pwm_frequency_hz(self, frequency_hz: int) -> None` | Validate, persist, and immediately apply PWM frequency. |
| `reset_cc_vref_pwm_frequency_hz` | `async def reset_cc_vref_pwm_frequency_hz(self) -> None` | Restore and persist build-time PWM frequency default. |


### `t76.drpd.device.device_system.DeviceSystemConfiguration`

**Kind:** class

System configuration namespace.





### `t76.drpd.device.device_system.DeviceSystem`

**Kind:** class

Represents the system-related commands for a DRPD device.




#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `identify` | `async def identify(self) -> DeviceInfo` | Identify the device. |
| `reset` | `async def reset(self) -> None` | Reset the instrument to its power-on state. |
| `get_hardware_revision` | `async def get_hardware_revision(self) -> str` | Query the detected hardware revision. |
| `enter_firmware_updater` | `async def enter_firmware_updater(self) -> None` | Reboot into the resident firmware updater bootloader. |
| `get_memory_usage` | `async def get_memory_usage(self) -> MemoryUsage` | Get the memory usage of the device. |
| `get_clock_frequency` | `async def get_clock_frequency(self) -> int` | Get the clock frequency of the device. |
| `get_uptime` | `async def get_uptime(self) -> int` | Get the uptime of the device in seconds. |
| `get_timestamp` | `async def get_timestamp(self) -> str` | Get the current timestamp from the device. |


## `t76.drpd.device.device_test`

**Source:** `python/t76/drpd/device/device_test.py`

Protected test SCPI command group for DRPD devices.

### `t76.drpd.device.device_test.DeviceTest`

**Kind:** class

Provides access to firmware TEST:* SCPI commands.




#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `set_vbus_manager_state` | `async def set_vbus_manager_state(self, state: OnOffStatus) -> None` | — |
| `get_vbus_manager_state` | `async def get_vbus_manager_state(self) -> OnOffStatus` | — |
| `set_cc1_role` | `async def set_cc1_role(self, role: DiagnosticCCRole) -> None` | — |
| `get_cc1_role` | `async def get_cc1_role(self) -> DiagnosticCCRole` | — |
| `set_cc2_role` | `async def set_cc2_role(self, role: DiagnosticCCRole) -> None` | — |
| `get_cc2_role` | `async def get_cc2_role(self) -> DiagnosticCCRole` | — |
| `set_dut_channel` | `async def set_dut_channel(self, channel: DiagnosticCCChannel) -> None` | — |
| `get_dut_channel` | `async def get_dut_channel(self) -> DiagnosticCCChannel` | — |
| `set_usds_channel` | `async def set_usds_channel(self, channel: DiagnosticCCChannel) -> None` | — |
| `get_usds_channel` | `async def get_usds_channel(self) -> DiagnosticCCChannel` | — |
| `set_cc_mux_state` | `async def set_cc_mux_state(self, state: OnOffStatus) -> None` | — |
| `get_cc_mux_state` | `async def get_cc_mux_state(self) -> OnOffStatus` | — |


## `t76.drpd.device.device_trigger`

**Source:** `python/t76/drpd/device/device_trigger.py`

Copyright (c) 2025 MTA, Inc.

The Device class enables communication with DRPD devices
over USB using SCPI commands.

### `t76.drpd.device.device_trigger.TriggerInfo`

**Kind:** dataclass

**Decorators:** `dataclass`

Represents information about the trigger system of the DRPD device.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `status` | TriggerStatus | — |
| `type` | TriggerType | — |
| `event_threshold` | int | — |
| `sender_filter` | TriggerSenderFilter | — |
| `autorepeat` | OnOffStatus | — |
| `event_count` | int | — |
| `sync_mode` | TriggerSyncMode | — |
| `sync_pulse_length` | int | — |
| `message_type_filters` | list[TriggerMessageTypeFilter] | — |




### `t76.drpd.device.device_trigger.DeviceTrigger`

**Kind:** class

Represents the trigger system of the DRPD device.




#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `reset` | `async def reset(self) -> None` | Reset the trigger system of the device. |
| `get_status` | `async def get_status(self) -> TriggerStatus` | Get the current trigger status of the device. |
| `set_type` | `async def set_type(self, trigger_type: TriggerType) -> None` | Set the trigger type of the device. |
| `get_type` | `async def get_type(self) -> TriggerType` | Get the current trigger type of the device. |
| `set_event_threshold` | `async def set_event_threshold(self, count: int) -> None` | Set the event count threshold for the trigger. |
| `get_event_threshold` | `async def get_event_threshold(self) -> int` | Get the current event count threshold for the trigger. |
| `set_sender_filter` | `async def set_sender_filter(self, sender: TriggerSenderFilter) -> None` | Set the trigger sender filter. |
| `get_sender_filter` | `async def get_sender_filter(self) -> TriggerSenderFilter` | Query the trigger sender filter. |
| `set_message_type_filters` | `async def set_message_type_filters(self, filters: list[TriggerMessageTypeFilter]) -> None` | Replace all trigger message-type filters. |
| `get_message_type_filters` | `async def get_message_type_filters(self) -> list[TriggerMessageTypeFilter]` | Query all configured trigger message-type filters. |
| `clear_message_type_filters` | `async def clear_message_type_filters(self) -> None` | Clear all trigger message-type filters. |
| `set_autorepeat` | `async def set_autorepeat(self, enable: OnOffStatus) -> None` | Enable or disable trigger autorepeat. |
| `get_autorepeat` | `async def get_autorepeat(self) -> OnOffStatus` | Get the current status of trigger autorepeat. |
| `get_event_count` | `async def get_event_count(self) -> int` | Get the current event count. |
| `set_sync_mode` | `async def set_sync_mode(self, mode: TriggerSyncMode) -> None` | Set the trigger sync output mode. |
| `get_sync_mode` | `async def get_sync_mode(self) -> TriggerSyncMode` | Get the current trigger sync output mode. |
| `set_sync_pulse_length` | `async def set_sync_pulse_length(self, length_us: int) -> None` | Set the trigger output pulse length in microseconds. |
| `get_sync_pulse_length` | `async def get_sync_pulse_length(self) -> int` | Get the current trigger output pulse length in microseconds. |
| `get_trigger_info` | `async def get_trigger_info(self) -> TriggerInfo` | Get comprehensive information about the trigger system. |


## `t76.drpd.device.device_vbus`

**Source:** `python/t76/drpd/device/device_vbus.py`

Copyright (c) 2025 MTA, Inc.

The Device class enables communication with DRPD devices
over USB using SCPI commands.

### `t76.drpd.device.device_vbus.VBusInfo`

**Kind:** dataclass

**Decorators:** `dataclass`

Represents information about the VBus system of the DRPD device.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `state` | VBusState | — |
| `ovp_threshold` | float | — |
| `ocp_threshold` | float | — |
| `ovp_event_timestamp_us` | int \| None | — |
| `ocp_event_timestamp_us` | int \| None | — |




### `t76.drpd.device.device_vbus.DeviceVBus`

**Kind:** class

Represents the VBus system of the DRPD device.




#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `reset` | `async def reset(self) -> None` | Reset the device's VBus |
| `get_state` | `async def get_state(self) -> VBusState` | Get the current VBus status of the device. |
| `get_ovp_threshold` | `async def get_ovp_threshold(self) -> float` | Get the Over Voltage Protection (OVP) threshold. |
| `set_ovp_threshold` | `async def set_ovp_threshold(self, threshold: float) -> None` | Set the Over Voltage Protection (OVP) threshold. |
| `get_ocp_threshold` | `async def get_ocp_threshold(self) -> float` | Get the Over Current Protection (OCP) threshold. |
| `set_ocp_threshold` | `async def set_ocp_threshold(self, threshold: float) -> None` | Set the Over Current Protection (OCP) threshold. |
| `get_info` | `async def get_info(self) -> VBusInfo` | Get comprehensive information about the VBus system. |


## `t76.drpd.device.discovery`

**Source:** `python/t76/drpd/device/discovery.py`

Copyright (c) 2025 MTA, Inc.

The discovery module provides functionality to discover DRPD devices connected via USB.

### `t76.drpd.device.discovery.find_drpd_devices`

**Kind:** function

```python
def find_drpd_devices() -> List[Device]
```

Discover DRPD devices connected via USB.

Returns:
    list: A list of discovered DRPD devices.





## `t76.drpd.device.events`

**Source:** `python/t76/drpd/device/events.py`

Copyright (c) 2025 MTA, Inc.

Events raised by the DRPD device.

### `t76.drpd.device.events.DeviceEvent`

**Kind:** class

No docstring.





### `t76.drpd.device.events.InterruptReceived`

**Kind:** class

**Bases:** `DeviceEvent`

No docstring.





### `t76.drpd.device.events.DeviceConnected`

**Kind:** class

**Bases:** `DeviceEvent`

No docstring.





### `t76.drpd.device.events.DeviceDisconnected`

**Kind:** class

**Bases:** `DeviceEvent`

No docstring.





### `t76.drpd.device.events.TriggerStatusChanged`

**Kind:** class

**Bases:** `DeviceEvent`

No docstring.





### `t76.drpd.device.events.CCBusStateChanged`

**Kind:** class

**Bases:** `DeviceEvent`

No docstring.





### `t76.drpd.device.events.RoleChanged`

**Kind:** class

**Bases:** `DeviceEvent`

No docstring.





### `t76.drpd.device.events.VBusManagerStateChanged`

**Kind:** class

**Bases:** `DeviceEvent`

No docstring.





### `t76.drpd.device.events.CaptureStatusChanged`

**Kind:** class

**Bases:** `DeviceEvent`

No docstring.





### `t76.drpd.device.events.SinkPDOListChanged`

**Kind:** class

**Bases:** `DeviceEvent`

No docstring.





### `t76.drpd.device.events.SinkInfoChanged`

**Kind:** class

**Bases:** `DeviceEvent`

No docstring.





### `t76.drpd.device.events.BMCSequenceCaptured`

**Kind:** class

**Bases:** `DeviceEvent`

No docstring.





### `t76.drpd.device.events.FirmwareEventCaptured`

**Kind:** class

**Bases:** `DeviceEvent`

No docstring.





### `t76.drpd.device.events.AnalogMonitorStatusChanged`

**Kind:** class

**Bases:** `DeviceEvent`

No docstring.





## `t76.drpd.device.types`

**Source:** `python/t76/drpd/device/types.py`

Copyright (c) 2025 MTA, Inc.

Types and Enums for DRPD device communication.

### `t76.drpd.device.types.DeviceInfo`

**Kind:** dataclass

**Decorators:** `dataclass`

Represents the basic information of the device.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `manufacturer` | str | — |
| `model` | str | — |
| `serial_number` | str | — |
| `firmware_version` | str | — |
| `hardware_revision` | str \| None | None |




### `t76.drpd.device.types.DeviceStatusFlags`

**Kind:** class

**Bases:** `enum.Flag`

Represents the status flags of the device.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `NONE` | — | 0 |
| `VBUS_STATUS_CHANGED` | — | enum.auto() |
| `ROLE_CHANGED` | — | enum.auto() |
| `CAPTURE_STATUS_CHANGED` | — | enum.auto() |
| `CC_BUS_STATUS_CHANGED` | — | enum.auto() |
| `TRIGGER_STATUS_CHANGED` | — | enum.auto() |
| `SINK_PDO_LIST_CHANGED` | — | enum.auto() |
| `SINK_STATUS_CHANGED` | — | enum.auto() |
| `MESSAGE_RECEIVED` | — | enum.auto() |




### `t76.drpd.device.types.AnalogMonitorCCChannelStatus`

**Kind:** enum

**Bases:** `enum.Enum`

Represents the status of the analog monitor CC channels.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `UNKNOWN` | — | 'Unknown' |
| `SINK_TX_NG` | — | 'SinkTxNG' |
| `SINK_TX_OK` | — | 'SinkTxOK' |
| `V_CONN` | — | 'VConn' |
| `DISCONNECTED` | — | 'Disconnected' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `status_from_voltage` | `def status_from_voltage(voltage: float) -> 'AnalogMonitorCCChannelStatus'` | Determine the status based on the voltage level. |


### `t76.drpd.device.types.AccumulatedMeasurements`

**Kind:** dataclass

**Decorators:** `dataclass`

Represents accumulated VBUS charge and energy counters.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `accumulation_elapsed_time_us` | int | — |
| `accumulated_charge_mah` | int | — |
| `accumulated_energy_mwh` | int | — |




### `t76.drpd.device.types.AnalogMonitorChannels`

**Kind:** dataclass

**Decorators:** `dataclass`

Represents the analog monitor channels and their voltages.

The VBUS capture timestamp is in microseconds when provided by the
device firmware. Accumulation values are reported as elapsed time in
microseconds plus absolute charge and energy counters.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `vbus_timestamp_us` | int \| None | — |
| `dut_cc1` | float | — |
| `dut_cc2` | float | — |
| `usds_cc1` | float | — |
| `usds_cc2` | float | — |
| `vbus` | float | — |
| `ibus` | float | — |
| `adc_vref` | float | — |
| `ground_ref` | float | — |
| `current_vref` | float | — |
| `accumulation_elapsed_time_us` | int \| None | — |
| `accumulated_charge_mah` | int \| None | — |
| `accumulated_energy_mwh` | int \| None | — |


#### Public properties

| Property | Type | Access | Summary |
| --- | --- | --- | --- |
| `dut_cc1_status` | AnalogMonitorCCChannelStatus | read-only | — |
| `dut_cc2_status` | AnalogMonitorCCChannelStatus | read-only | — |
| `usds_cc1_status` | AnalogMonitorCCChannelStatus | read-only | — |
| `usds_cc2_status` | AnalogMonitorCCChannelStatus | read-only | — |



### `t76.drpd.device.types.Mode`

**Kind:** enum

**Bases:** `enum.Enum`

Represents the mode of the device.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `UNKNOWN` | — | 'UNKNOWN' |
| `DISABLED` | — | 'DISABLED' |
| `OBSERVER` | — | 'OBSERVER' |
| `SINK` | — | 'SINK' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, mode_str: str) -> 'Mode'` | Convert a string to a Mode enum. |


### `t76.drpd.device.types.CCBusState`

**Kind:** enum

**Bases:** `enum.Enum`

Represents the status of the CC bus on the device.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `UNATTACHED` | — | 'UNATTACHED' |
| `SOURCE_FOUND` | — | 'SOURCE_FOUND' |
| `ATTACHED` | — | 'ATTACHED' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, status_str: str) -> 'CCBusState'` | Convert a string to a Status enum. |


### `t76.drpd.device.types.CCChannel`

**Kind:** enum

**Bases:** `enum.Enum`

Represents the ports of the device.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `DUT_CC1` | — | 'DUTCC1' |
| `DUT_CC2` | — | 'DUTCC2' |
| `USDS_CC1` | — | 'USDSCC1' |
| `USDS_CC2` | — | 'USDSCC2' |
| `NONE` | — | 'NONE' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_int` | `def from_int(cls, port_int: int) -> 'CCChannel'` | Convert an integer to a Port enum. |
| `from_string` | `def from_string(cls, port_str: str) -> 'CCChannel'` | Convert a string to a Port enum. |


### `t76.drpd.device.types.ResistorStatus`

**Kind:** enum

**Bases:** `enum.Enum`

Represents the resistor status of a port.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `UNKNOWN` | — | 'UNKNOWN' |
| `OBSERVER` | — | 'OBSERVER' |
| `CABLE` | — | 'CABLE' |
| `VCONN_5V` | — | 'VCONN_5V' |
| `VCONN_3V3` | — | 'VCONN_3V3' |
| `SINK` | — | 'SINK' |
| `SOURCE_3A` | — | 'SOURCE_3A' |
| `SOURCE_1_5A` | — | 'SOURCE_1_5A' |
| `SOURCE_DEFAULT` | — | 'SOURCE_DEFAULT' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, status_str: str) -> 'ResistorStatus'` | Convert a string to a ResistorStatus enum. |


### `t76.drpd.device.types.MemoryUsage`

**Kind:** dataclass

**Decorators:** `dataclass`

Represents the memory usage of the device, in bytes.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `total` | int | — |
| `free` | int | — |




### `t76.drpd.device.types.OnOffStatus`

**Kind:** enum

**Bases:** `enum.Enum`

Represents the status of the capture.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `ON` | — | True |
| `OFF` | — | False |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, status_str: str) -> 'OnOffStatus'` | Convert a string to a CaptureStatus enum. |
| `from_bool` | `def from_bool(cls, status_bool: bool) -> 'OnOffStatus'` | Convert a boolean to an OnOffStatus enum. |


### `t76.drpd.device.types.TriggerStatus`

**Kind:** enum

**Bases:** `enum.Enum`

Represents the trigger status of the device.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `IDLE` | — | 'IDLE' |
| `ARMED` | — | 'ARMED' |
| `TRIGGERED` | — | 'TRIGGERED' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, status_str: str) -> 'TriggerStatus'` | Convert a string to a TriggerStatus enum. |


### `t76.drpd.device.types.TriggerType`

**Kind:** enum

**Bases:** `enum.Enum`

Represents the trigger types of the device.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `OFF` | — | 'OFF' |
| `PREAMBLE_START` | — | 'PREAMBLE_START' |
| `SOP_START` | — | 'SOP_START' |
| `HEADER_START` | — | 'HEADER_START' |
| `DATA_START` | — | 'DATA_START' |
| `MESSAGE_COMPLETE` | — | 'MESSAGE_COMPLETE' |
| `HARD_RESET_RECEIVED` | — | 'HARD_RESET_RECEIVED' |
| `INVALID_KCODE` | — | 'INVALID_KCODE' |
| `CRC_ERROR` | — | 'CRC_ERROR' |
| `TIMEOUT_ERROR` | — | 'TIMEOUT_ERROR' |
| `RUNT_PULSE_ERROR` | — | 'RUNT_PULSE_ERROR' |
| `ANY_ERROR` | — | 'ANY_ERROR' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, type_str: str) -> 'TriggerType'` | Convert a string to a TriggerType enum. |


### `t76.drpd.device.types.TriggerSyncMode`

**Kind:** enum

**Bases:** `enum.Enum`

Represents the trigger output modes of the device.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `PULSE_HIGH` | — | 'PULSE_HIGH' |
| `PULSE_LOW` | — | 'PULSE_LOW' |
| `TOGGLE` | — | 'TOGGLE' |
| `PULL_DOWN` | — | 'PULL_DOWN' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, mode_str: str) -> 'TriggerSyncMode'` | Convert a string to a TriggerOutputMode enum. |


### `t76.drpd.device.types.TriggerSenderFilter`

**Kind:** enum

**Bases:** `enum.Enum`

Represents the trigger sender filter.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `ANY` | — | 'ANY' |
| `SOURCE` | — | 'SOURCE' |
| `SINK` | — | 'SINK' |
| `CABLE` | — | 'CABLE' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, filter_str: str) -> 'TriggerSenderFilter'` | — |


### `t76.drpd.device.types.TriggerMessageTypeFilterClass`

**Kind:** enum

**Bases:** `enum.Enum`

Represents a trigger message-type filter class.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `CONTROL` | — | 'CONTROL' |
| `DATA` | — | 'DATA' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, class_str: str) -> 'TriggerMessageTypeFilterClass'` | — |


### `t76.drpd.device.types.TriggerMessageTypeFilter`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Represents one trigger message-type filter slot.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `filter_class` | TriggerMessageTypeFilterClass | — |
| `message_type_number` | int | — |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, value: str) -> 'TriggerMessageTypeFilter'` | — |
| `to_scpi` | `def to_scpi(self) -> str` | — |


### `t76.drpd.device.types.VBusState`

**Kind:** enum

**Bases:** `enum.Enum`

Represents the VBus state of the device.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `DISABLED` | — | 'DISABLED' |
| `ENABLED` | — | 'ENABLED' |
| `OVP` | — | 'OVP' |
| `OCP` | — | 'OCP' |
| `UNKNOWN` | — | 'UNKNOWN' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, state_str: str) -> 'VBusState'` | Convert a string to a VBusState enum. |


### `t76.drpd.device.types.SinkState`

**Kind:** enum

**Bases:** `enum.Enum`

Represents the Sink state of the device.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `DISCONNECTED` | — | 'DISCONNECTED' |
| `PE_SNK_STARTUP` | — | 'PE_SNK_STARTUP' |
| `PE_SNK_DISCOVERY` | — | 'PE_SNK_DISCOVERY' |
| `PE_SNK_WAIT_FOR_CAPABILITIES` | — | 'PE_SNK_WAIT_FOR_CAPABILITIES' |
| `PE_SNK_EVALUATE_CAPABILITY` | — | 'PE_SNK_EVALUATE_CAPABILITY' |
| `PE_SNK_SELECT_CAPABILITY` | — | 'PE_SNK_SELECT_CAPABILITY' |
| `PE_SNK_TRANSITION_SINK` | — | 'PE_SNK_TRANSITION_SINK' |
| `PE_SNK_READY` | — | 'PE_SNK_READY' |
| `PE_SNK_SEND_EPR_MODE_ENTRY` | — | 'PE_SNK_SEND_EPR_MODE_ENTRY' |
| `PE_SNK_EPR_MODE_WAIT_FOR_RESPONSE` | — | 'PE_SNK_EPR_MODE_WAIT_FOR_RESPONSE' |
| `PE_SNK_SEND_EPR_MODE_EXIT` | — | 'PE_SNK_SEND_EPR_MODE_EXIT' |
| `PE_SNK_GIVE_SINK_CAP` | — | 'PE_SNK_GIVE_SINK_CAP' |
| `PE_SNK_GET_SOURCE_CAP` | — | 'PE_SNK_GET_SOURCE_CAP' |
| `PE_SNK_GET_PPS_STATUS` | — | 'PE_SNK_GET_PPS_STATUS' |
| `PE_SNK_INQUIRY` | — | 'PE_SNK_INQUIRY' |
| `PE_SNK_EPR_KEEPALIVE` | — | 'PE_SNK_EPR_KEEPALIVE' |
| `PE_SNK_HARD_RESET` | — | 'PE_SNK_HARD_RESET' |
| `PE_SNK_TRANSITION_TO_DEFAULT` | — | 'PE_SNK_TRANSITION_TO_DEFAULT' |
| `ERROR` | — | 'ERROR' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, state_str: str) -> 'SinkState'` | Convert a string to a SinkState enum. |


### `t76.drpd.device.types.SinkRequestOutcome`

**Kind:** enum

**Bases:** `enum.Enum`

Represents a SINK:REQUEST:STATUS? outcome token.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `NONE` | — | 'NONE' |
| `PENDING` | — | 'PENDING' |
| `ACCEPTED` | — | 'ACCEPTED' |
| `REJECTED` | — | 'REJECTED' |
| `WAIT` | — | 'WAIT' |
| `NOT_SUPPORTED` | — | 'NOT_SUPPORTED' |
| `TIMEOUT` | — | 'TIMEOUT' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, outcome_str: str) -> 'SinkRequestOutcome'` | — |


### `t76.drpd.device.types.SinkRequestStatus`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Represents the most recent SINK:PDO request outcome.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `outcome` | SinkRequestOutcome | — |
| `index` | int \| None | — |
| `voltage_mv` | int \| None | — |
| `current_ma` | int \| None | — |




### `t76.drpd.device.types.SinkInquiryType`

**Kind:** enum

**Bases:** `enum.Enum`

Supported Sink-to-Source inquiry message types.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `GET_SOURCE_CAP` | — | 'GET_SOURCE_CAP' |
| `GET_SOURCE_CAP_EXTENDED` | — | 'GET_SOURCE_CAP_EXTENDED' |
| `GET_STATUS` | — | 'GET_STATUS' |
| `GET_REVISION` | — | 'GET_REVISION' |
| `GET_SOURCE_INFO` | — | 'GET_SOURCE_INFO' |
| `GET_PPS_STATUS` | — | 'GET_PPS_STATUS' |
| `GET_MANUFACTURER_INFO` | — | 'GET_MANUFACTURER_INFO' |
| `GET_COUNTRY_CODES` | — | 'GET_COUNTRY_CODES' |
| `GET_COUNTRY_INFO` | — | 'GET_COUNTRY_INFO' |
| `GET_BATTERY_CAP` | — | 'GET_BATTERY_CAP' |
| `GET_BATTERY_STATUS` | — | 'GET_BATTERY_STATUS' |
| `DISCOVER_IDENTITY` | — | 'DISCOVER_IDENTITY' |
| `DISCOVER_SVIDS` | — | 'DISCOVER_SVIDS' |
| `DISCOVER_MODES` | — | 'DISCOVER_MODES' |
| `GET_DIGESTS` | — | 'GET_DIGESTS' |
| `GET_CERTIFICATE` | — | 'GET_CERTIFICATE' |
| `CHALLENGE` | — | 'CHALLENGE' |




### `t76.drpd.device.types.ManufacturerInfoTarget`

**Kind:** enum

**Bases:** `enum.Enum`

Semantic Manufacturer_Info target.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `PORT` | — | 'PORT' |
| `BATTERY` | — | 'BATTERY' |




### `t76.drpd.device.types.BatteryReferenceKind`

**Kind:** enum

**Bases:** `enum.Enum`

Meaning of a USB-PD battery reference.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `FIXED` | — | 'FIXED' |
| `HOT_SWAPPABLE` | — | 'HOT_SWAPPABLE' |




### `t76.drpd.device.types.CablePlug`

**Kind:** enum

**Bases:** `enum.Enum`

Explicit cable-message target; SOP fallback is forbidden.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `SOP_PRIME` | — | 'SOP_PRIME' |
| `SOP_DOUBLE_PRIME` | — | 'SOP_DOUBLE_PRIME' |




### `t76.drpd.device.types.battery_reference_kind`

**Kind:** function

```python
def battery_reference_kind(reference: int) -> BatteryReferenceKind
```

Map references 0..3 to fixed and 4..7 to hot-swappable.





### `t76.drpd.device.types.GetSourceCapabilitiesInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Request current SPR Source Capabilities.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_SOURCE_CAP, init=False) |




### `t76.drpd.device.types.GetExtendedSourceCapabilitiesInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Request the PD 3.x Source Capabilities Extended data block.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_SOURCE_CAP_EXTENDED, init=False) |




### `t76.drpd.device.types.GetStatusInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Request the PD 3.x Source Status data block.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_STATUS, init=False) |




### `t76.drpd.device.types.GetRevisionInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Semantic request for the Source's USB-PD Revision data message.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_REVISION, init=False) |




### `t76.drpd.device.types.GetSourceInfoInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Request the PD 3.x Source Info data object.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_SOURCE_INFO, init=False) |




### `t76.drpd.device.types.GetPPSStatusInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Request PPS Status; firmware validates active SPR PPS applicability.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_PPS_STATUS, init=False) |




### `t76.drpd.device.types.GetManufacturerInfoInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Request Port or Battery manufacturer information.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `target` | ManufacturerInfoTarget | ManufacturerInfoTarget.PORT |
| `battery_reference` | int \| None | None |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_MANUFACTURER_INFO, init=False) |




### `t76.drpd.device.types.GetCountryCodesInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Request supported ISO 3166 alpha-2 country codes.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_COUNTRY_CODES, init=False) |




### `t76.drpd.device.types.GetCountryInfoInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Request information for one ISO 3166 alpha-2 country code.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `country_code` | str | — |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_COUNTRY_INFO, init=False) |




### `t76.drpd.device.types.GetBatteryCapabilitiesInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Request capabilities for fixed ref 0..3 or hot-swappable ref 4..7.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `battery_reference` | int | — |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_BATTERY_CAP, init=False) |


#### Public properties

| Property | Type | Access | Summary |
| --- | --- | --- | --- |
| `reference_kind` | BatteryReferenceKind | read-only | — |
| `slot_index` | int | read-only | — |



### `t76.drpd.device.types.GetBatteryStatusInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Request live status for fixed ref 0..3 or hot-swappable ref 4..7.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `battery_reference` | int | — |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_BATTERY_STATUS, init=False) |


#### Public properties

| Property | Type | Access | Summary |
| --- | --- | --- | --- |
| `reference_kind` | BatteryReferenceKind | read-only | — |
| `slot_index` | int | read-only | — |



### `t76.drpd.device.types.DiscoverIdentityInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Diagnostic Identity request from current UFP/Sink to SOP partner.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `type` | SinkInquiryType | field(default=SinkInquiryType.DISCOVER_IDENTITY, init=False) |




### `t76.drpd.device.types.DiscoverSVIDsInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Optional UFP-initiated SVID discovery of the SOP Port Partner.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `type` | SinkInquiryType | field(default=SinkInquiryType.DISCOVER_SVIDS, init=False) |




### `t76.drpd.device.types.DiscoverModesInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Optional UFP-initiated Modes discovery for one partner SVID.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `svid` | int | — |
| `type` | SinkInquiryType | field(default=SinkInquiryType.DISCOVER_MODES, init=False) |




### `t76.drpd.device.types.CableStatusInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Request Status from an explicitly selected active-cable plug.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `plug` | CablePlug | — |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_STATUS, init=False) |




### `t76.drpd.device.types.CableRevisionInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Request Revision from an explicitly selected cable plug.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `plug` | CablePlug | — |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_REVISION, init=False) |




### `t76.drpd.device.types.CableManufacturerInfoInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Request cable-plug Manufacturer Info using Port/CablePlug target.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `plug` | CablePlug | — |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_MANUFACTURER_INFO, init=False) |




### `t76.drpd.device.types.CableDiscoverIdentityInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Discover Identity from an explicitly selected cable plug.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `plug` | CablePlug | — |
| `type` | SinkInquiryType | field(default=SinkInquiryType.DISCOVER_IDENTITY, init=False) |




### `t76.drpd.device.types.CableDiscoverSVIDsInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Discover optional SVIDs from an explicitly selected cable plug.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `plug` | CablePlug | — |
| `type` | SinkInquiryType | field(default=SinkInquiryType.DISCOVER_SVIDS, init=False) |




### `t76.drpd.device.types.CableDiscoverModesInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Discover optional Modes for a selected SVID and cable plug.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `plug` | CablePlug | — |
| `svid` | int | — |
| `type` | SinkInquiryType | field(default=SinkInquiryType.DISCOVER_MODES, init=False) |




### `t76.drpd.device.types.GetDigestsInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Enumerate populated USB Type-C Authentication certificate slots.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_DIGESTS, init=False) |




### `t76.drpd.device.types.GetCertificateInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Retrieve one bounded part of a selected authentication certificate chain.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `slot` | int | — |
| `offset` | int | — |
| `length` | int | — |
| `type` | SinkInquiryType | field(default=SinkInquiryType.GET_CERTIFICATE, init=False) |




### `t76.drpd.device.types.ChallengeInquiryRequest`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Authenticate a selected slot using one caller-generated fresh nonce.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `slot` | int | — |
| `nonce` | bytes | — |
| `type` | SinkInquiryType | field(default=SinkInquiryType.CHALLENGE, init=False) |




### `t76.drpd.device.types.SinkInquiryOutcome`

**Kind:** enum

**Bases:** `enum.Enum`

Represents a SINK:INQuiry:STATus? outcome token.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `NONE` | — | 'NONE' |
| `PENDING` | — | 'PENDING' |
| `RESPONSE` | — | 'RESPONSE' |
| `NOT_SUPPORTED` | — | 'NOT_SUPPORTED' |
| `REJECTED` | — | 'REJECTED' |
| `WAIT` | — | 'WAIT' |
| `GOODCRC_TIMEOUT` | — | 'GOODCRC_TIMEOUT' |
| `RESPONSE_TIMEOUT` | — | 'RESPONSE_TIMEOUT' |
| `PROTOCOL_ERROR` | — | 'PROTOCOL_ERROR' |
| `MALFORMED_RESPONSE` | — | 'MALFORMED_RESPONSE' |
| `RESPONSE_TOO_LARGE` | — | 'RESPONSE_TOO_LARGE' |
| `ABORTED` | — | 'ABORTED' |
| `NAK` | — | 'NAK' |
| `BUSY` | — | 'BUSY' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, outcome_str: str) -> 'SinkInquiryOutcome'` | — |


### `t76.drpd.device.types.SinkInquiryStatus`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Most recent Sink-to-Source inquiry status.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `outcome` | SinkInquiryOutcome | — |
| `request_id` | int | — |
| `type` | SinkInquiryType | — |
| `response_class` | int | — |
| `response_type` | int | — |
| `response_length` | int | — |




### `t76.drpd.device.types.SourceCapabilitiesInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Decoded Source_Capabilities logical body.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `pdos` | tuple[SourcePDO, ...] | — |




### `t76.drpd.device.types.ExtendedSourceCapabilitiesInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Decoded Source Capabilities Extended data block.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `payload_length` | int | — |
| `vendor_id` | int | — |
| `product_id` | int | — |
| `xid` | int | — |
| `firmware_version` | int | — |
| `hardware_version` | int | — |
| `voltage_regulation` | int | — |
| `holdup_time_ms` | int | — |
| `compliance` | int | — |
| `touch_current` | int | — |
| `peak_current` | tuple[int, int, int] | — |
| `touch_temperature` | int | — |
| `source_inputs` | int | — |
| `hot_swappable_battery_slots` | int | — |
| `fixed_batteries` | int | — |
| `spr_source_pdp_w` | int | — |
| `epr_source_pdp_w` | int \| None | — |
| `has_epr_source_pdp` | bool | — |




### `t76.drpd.device.types.SourceStatusInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Decoded SOP Status data block.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `payload_length` | int | — |
| `internal_temperature` | int | — |
| `present_input` | int | — |
| `present_battery_input` | int | — |
| `event_flags` | int | — |
| `temperature_status` | int | — |
| `power_status` | int | — |
| `power_state` | int \| None | — |
| `has_power_state_change` | bool | — |
| `over_current_event` | bool | — |
| `over_temperature_event` | bool | — |
| `over_voltage_event` | bool | — |
| `operating_in_current_limit` | bool | — |




### `t76.drpd.device.types.RevisionInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Decoded Revision Message data object.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `revision_major` | int | — |
| `revision_minor` | int | — |
| `version_major` | int | — |
| `version_minor` | int | — |




### `t76.drpd.device.types.SourceInfoInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Decoded Source Info data object; PDP values are watts.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `port_type` | int | — |
| `port_maximum_pdp_w` | int | — |
| `port_present_pdp_w` | int | — |
| `port_reported_pdp_w` | int | — |




### `t76.drpd.device.types.PPSStatusInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Decoded four-byte PPS Status data block.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `output_voltage_mv` | int \| None | — |
| `output_current_ma` | int \| None | — |
| `present_temperature_flag` | int | — |
| `operating_in_current_limit` | bool | — |
| `real_time_flags` | int | — |




### `t76.drpd.device.types.ManufacturerInfoInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Decoded Manufacturer Info data block.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `vendor_id` | int | — |
| `product_id` | int | — |
| `manufacturer_string` | str | — |
| `manufacturer_string_bytes` | bytes | — |




### `t76.drpd.device.types.CountryCodesInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Decoded Country Codes data block.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `country_codes` | tuple[str, ...] | — |




### `t76.drpd.device.types.CountryInfoInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Decoded Country Info data block correlated to its request.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `country_code` | str | — |
| `country_specific_data` | bytes | — |




### `t76.drpd.device.types.BatteryCapacityMeaning`

**Kind:** enum

**Bases:** `enum.Enum`

Meaning of a 0.1 Wh Battery_Capabilities capacity field.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `VALUE` | — | 'VALUE' |
| `BATTERY_NOT_PRESENT` | — | 'BATTERY_NOT_PRESENT' |
| `UNKNOWN` | — | 'UNKNOWN' |




### `t76.drpd.device.types.BatteryCapacity`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Exact Battery_Capabilities capacity value and sentinel meaning.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `raw_tenths_wh` | int | — |
| `meaning` | BatteryCapacityMeaning | — |


#### Public properties

| Property | Type | Access | Summary |
| --- | --- | --- | --- |
| `watt_hours` | Decimal \| None | read-only | — |



### `t76.drpd.device.types.BatteryCapabilitiesInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Decoded nine-byte Battery Capabilities data block.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `vendor_id` | int | — |
| `product_id` | int | — |
| `design_capacity` | BatteryCapacity | — |
| `last_full_charge_capacity` | BatteryCapacity | — |
| `battery_type_raw` | int | — |
| `invalid_battery_reference` | bool | — |
| `battery_present` | bool | — |




### `t76.drpd.device.types.BatteryChargingState`

**Kind:** enum

**Bases:** `enum.Enum`

Battery_Status charging-state field.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `CHARGING` | — | 0 |
| `DISCHARGING` | — | 1 |
| `IDLE` | — | 2 |
| `RESERVED` | — | 3 |




### `t76.drpd.device.types.BatteryStatusInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Decoded Battery Status Data Object.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `present_capacity_raw_tenths_wh` | int | — |
| `present_capacity_meaning` | BatteryCapacityMeaning | — |
| `present_capacity_wh` | Decimal \| None | — |
| `invalid_battery_reference` | bool | — |
| `battery_present` | bool | — |
| `charging_state` | BatteryChargingState | — |




### `t76.drpd.device.types.CableStatusInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Decoded two-byte SOP'/SOP'' Status Data Block.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `internal_temperature_raw` | int | — |
| `internal_temperature_c` | int \| None | — |
| `below_2_c` | bool | — |
| `flags_raw` | int | — |
| `thermal_shutdown` | bool | — |




### `t76.drpd.device.types.AuthenticationDigestsInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Validated slot mask and SHA-256 digest values in slot order.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `slot_mask` | int | — |
| `digests` | tuple[tuple[int, bytes], ...] | — |




### `t76.drpd.device.types.AuthenticationCertificateInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

One correlated, progress-making certificate-chain part.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `slot` | int | — |
| `offset` | int | — |
| `certificate_part` | bytes | — |




### `t76.drpd.device.types.AuthenticationChallengeInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Correlated CHALLENGE_AUTH transcript and little-endian signature.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `slot` | int | — |
| `signed_response` | bytes | — |
| `signature_little_endian` | bytes | — |




### `t76.drpd.device.types.AuthenticationErrorInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Protocol ERROR body; never represents authenticated success.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `code` | int | — |
| `data` | int | — |




### `t76.drpd.device.types.StructuredVDMHeaderData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Validated Structured VDM ACK header.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `raw` | int | — |
| `svid` | int | — |
| `version_major` | int | — |
| `version_minor` | int | — |
| `command` | int | — |




### `t76.drpd.device.types.StructuredVDMNegativeResponseData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Validated raw NAK or BUSY Structured VDM header.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `header` | StructuredVDMHeaderData | — |
| `outcome` | SinkInquiryOutcome | — |




### `t76.drpd.device.types.DiscoverIdentityInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Ordered raw Identity VDOs following the ACK header.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `header` | StructuredVDMHeaderData | — |
| `identity_vdos` | tuple[int, ...] | — |




### `t76.drpd.device.types.DiscoverSVIDsInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Ordered raw SVID VDOs plus stable first-occurrence deduplication.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `header` | StructuredVDMHeaderData | — |
| `svid_vdos` | tuple[int, ...] | — |
| `svids` | tuple[int, ...] | — |
| `complete` | bool | — |




### `t76.drpd.device.types.DiscoverModesInquiryData`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Ordered raw Mode VDOs correlated to selected SVID.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `header` | StructuredVDMHeaderData | — |
| `svid` | int | — |
| `mode_vdos` | tuple[int, ...] | — |




### `t76.drpd.device.types.SinkInquiryResult`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Correlated terminal inquiry result retained by the host runner.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `request` | SinkInquiryRequest | — |
| `status` | SinkInquiryStatus | — |
| `raw_response` | bytes \| None | — |
| `decoded` | SinkInquiryDecodedData \| None | None |




### `t76.drpd.device.types.CountryInquiryFailureAction`

**Kind:** enum

**Bases:** `enum.Enum`

Guided country-workflow handling for a terminal non-response.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `RETRY` | — | 'RETRY' |
| `CONTINUE` | — | 'CONTINUE' |
| `STOP` | — | 'STOP' |




### `t76.drpd.device.types.CountryInquiryWorkflowResult`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Host-retained result of a guided country-information workflow.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `country_codes_result` | SinkInquiryResult | — |
| `country_info_results` | tuple[SinkInquiryResult, ...] | — |
| `stopped_early` | bool | — |




### `t76.drpd.device.types.BatteryInquiryFailureAction`

**Kind:** enum

**Bases:** `enum.Enum`

Guided battery-survey handling for a terminal non-response.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `RETRY` | — | 'RETRY' |
| `CONTINUE` | — | 'CONTINUE' |
| `STOP` | — | 'STOP' |




### `t76.drpd.device.types.BatterySurveyResult`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Serialized Battery Capabilities/Status survey results.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `battery_references` | tuple[int, ...] | — |
| `inquiry_results` | tuple[SinkInquiryResult, ...] | — |
| `used_extended_source_counts` | bool | — |
| `stopped_early` | bool | — |




### `t76.drpd.device.types.VDMDiscoveryFailureAction`

**Kind:** enum

**Bases:** `enum.Enum`

Guided VDM discovery handling for NAK/BUSY/other non-ACK results.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `RETRY` | — | 'RETRY' |
| `CONTINUE` | — | 'CONTINUE' |
| `STOP` | — | 'STOP' |




### `t76.drpd.device.types.VDMDiscoveryWorkflowResult`

**Kind:** dataclass

**Decorators:** `dataclass(frozen=True)`

Serialized Identity, SVID, and Modes discovery history.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `identity_results` | tuple[SinkInquiryResult, ...] | — |
| `svid_results` | tuple[SinkInquiryResult, ...] | — |
| `mode_results` | tuple[SinkInquiryResult, ...] | — |
| `selected_svids` | tuple[int, ...] | — |
| `stopped_early` | bool | — |




### `t76.drpd.device.types.DiagnosticCCRole`

**Kind:** enum

**Bases:** `enum.Enum`

Represents TEST:CCROLE role values.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `SOURCE_DEFAULT` | — | 'SOURCE_DEFAULT' |
| `SOURCE_1_5A` | — | 'SOURCE_1_5A' |
| `SOURCE_3_0A` | — | 'SOURCE_3_0A' |
| `SINK` | — | 'SINK' |
| `EMARKER` | — | 'EMARKER' |
| `VCONN` | — | 'VCONN' |
| `OFF` | — | 'OFF' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, role_str: str) -> 'DiagnosticCCRole'` | — |


### `t76.drpd.device.types.DiagnosticCCChannel`

**Kind:** enum

**Bases:** `enum.Enum`

Represents TEST:CCBUS channel selections.


#### Fields

| Field | Type | Default |
| --- | --- | --- |
| `CC1` | — | 'CC1' |
| `CC2` | — | 'CC2' |



#### Public methods

| Method | Declaration | Summary |
| --- | --- | --- |
| `from_string` | `def from_string(cls, channel_str: str) -> 'DiagnosticCCChannel'` | — |


## `t76.drpd.device_reconciliation`

**Source:** `python/t76/drpd/device_reconciliation.py`

Helpers for selecting the active device from discovery results.

### `t76.drpd.device_reconciliation.choose_active_device`

**Kind:** function

```python
def choose_active_device(current_device: Optional[DeviceType], discovered_devices: Sequence[DeviceType], allow_auto_connect_single: bool) -> Optional[DeviceType]
```

Choose the next active device from the current discovery snapshot.
